overwrite: true
schema: "graphql/schema.json"
generates:
  src/generated/graphql.ts:
    config:
      withHooks: true
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
